<h4 class="card-title">Producción</h4>
<div class="row">
    <div class="col-6">
        <!-- residuos industriales -->   
        <div class="card">
            <div class="card-body">
                <div class="form-group">
                    {{ form_label(formulario.tieneResiduosIndustriales) }}
                    {{ form_widget(formulario.tieneResiduosIndustriales) }}
                </div> 
                <div id="divResiduosIndustriales">
                    <div class="form-group">
                        {{ form_label(formulario.tipoResiduoIndustrial) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.tipoResiduoIndustrial) }}
                    </div> 
                    <div class="form-group">
                        {{ form_label(formulario.residuoIndustrial) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.residuoIndustrial) }}
                    </div> 
                    <div class="form-group">
                        {{ form_label(formulario.tieneEfluentesLiquidos) }}
                        {{ form_widget(formulario.tieneEfluentesLiquidos) }}
                    </div> 
                    <div class="form-group" id="divTratamientoPrevioVuelco">
                        {{ form_label(formulario.tieneTratamientoPrevioVuelco) }}
                        {{ form_widget(formulario.tieneTratamientoPrevioVuelco) }}
                    </div> 
                    <div class="form-group">
                        {{ form_label(formulario.destinoVuelcoTipo) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.destinoVuelcoTipo) }}
                    </div> 
                </div>
            </div>
        </div>
    </div>
    <div class="col-6">
        <!-- residuos especiales -->
        <div class="card">
            <div class="card-body">
                <div class="form-group">
                    {{ form_label(formulario.tieneResiduosEspeciales) }}
                    {{ form_widget(formulario.tieneResiduosEspeciales) }}
                </div> 
                <div id="divResiduosEspeciales">
                    <div class="form-group">
                        {{ form_label(formulario.tipoResiduoEspecial) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.tipoResiduoEspecial) }}
                    </div> 
                    <div class="form-group">
                        {{ form_label(formulario.corrientes) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.corrientes) }}
                    </div> 
                    <div class="form-group">
                        {{ form_label(formulario.tieneEmisionesGaseosas) }}
                        {{ form_widget(formulario.tieneEmisionesGaseosas) }}
                    </div> 
                    <div class="form-group" id="divTipoEmisionGaseosa">
                        {{ form_label(formulario.tipoEmisionGaseosa) }}<span  class="text-danger">*</span>:
                        {{ form_widget(formulario.tipoEmisionGaseosa) }}
                    </div> 
                </div>
            </div>
        </div>
    </div>
</div>
<hr/>
<div class="row">
    <div class="col-6">
        <!-- varios -->
        <div class="card">
            <div class="card-body">
                <div class="form-group">
                    {{ form_label(formulario.potenciaUtilizada) }}<span  class="text-danger">*</span>:
                    {{ form_widget(formulario.potenciaUtilizada) }}
                </div> 
                <div class="form-group">
                    {{ form_label(formulario.CURT) }}<span  class="text-danger">*</span>:
                    {{ form_widget(formulario.CURT) }}
                </div> 
                <div class="form-group">
                    {{ form_label(formulario.fechaUltimaInpeccion) }}<span  class="text-danger">*</span>:
                    {{ form_widget(formulario.fechaUltimaInpeccion) }}
                </div> 
            </div>
        </div>
    </div>
    <div class="col-6">
        <!-- denuncias y reclamos -->
        <div class="card">
            <div class="card-body">
                <div class="form-group">
                    {{ form_label(formulario.tieneDenuncia) }}
                    {{ form_widget(formulario.tieneDenuncia) }}
                </div> 
                <div id="divEspecificacionDenuncia" class="form-group">
                    {{ form_label(formulario.denunciasEspecificaciones) }}<span  class="text-danger">*</span>:
                    {{ form_widget(formulario.denunciasEspecificaciones) }}
                </div> 
            </div>
        </div>
    </div>
</div>