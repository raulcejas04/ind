{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{parent()}}
    {{ encore_entry_link_tags('select2') }}  
    {{ encore_entry_link_tags('datepicker') }}
{% endblock %}
{% block title %}Nuevo lugar de producción y/o depósito{% endblock %}
{% block body %}
    <div class="container w-100 p-3">
        <div class="card mx-auto">
            <div class="card-header bg-info" >
                <h4 class="text-light mb-3 font-weight-normal">Nuevo lugar de producción y/o depósito</h4>     
            </div>
            <div class="card-body">  
                {{ include('lugar/_formulario.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('select2') }}  
    {{ encore_entry_script_tags('datepicker') }}  
    {{ encore_entry_script_tags('lugarForm') }}
{% endblock %}