{% extends 'base.html.twig' %}
{% block title %}Nuevo registro de {{ tipo.tipo }}{% endblock %}
{% block body %}
    <div class="container w-100 p-3">
        <div class="card mx-auto">
            <div class="card-header bg-info" >
                <h4 class="text-light mb-3 font-weight-normal">Nuevo registro de {{ tipo.tipo }}</h4>     
            </div>
            <div class="card-body">  
                {{ include('general/_formulario.html.twig',{'editar':'false'}) }}
            </div>
        </div>
    </div>
{% endblock %}