{% extends 'base.html.twig' %}


{% block title %}Editar {{general.descripcion}}{% endblock %}

{% block body %}

    <div class="container w-100 p-3">
        <div class="card mx-auto">
            <div class="card-header bg-info" >
                <h4 class="text-light mb-3 font-weight-normal">Editar</h4>     
            </div>
            <div class="card-body">
                {{ include('general/_formulario.html.twig', {'button_label': 'Guardar Cambios','editar':'true'}) }}
            </div>
        </div>
    </div>


{% endblock %}
