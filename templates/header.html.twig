
<nav class="navbar navbar-expand-lg navbar-light bg-info">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_GOD') %}
                <li class="nav-item active pr-2">
                    <a class="btn btn-sm btn-light" href="{{ path('admin_usuarios') }}">Administración de usuarios</a>
                </li>
                <li class="nav-item active pr-2">
                    <a class="btn btn-sm btn-light" href="{{ path('tablero_dashboard') }}">Dashboard</a>
                </li> 
            {% elseif is_granted('ROLE_ADMIN') %}                
                <li class="nav-item active pr-2">
                    <a class="btn btn-sm btn-light" href="{{ path('tablero_dashboard') }}">Dashboard</a>
                </li>   
            {% endif %}
        </ul>

        <div class="btn-toolbar"> 
            {% if is_granted('ROLE_USER') %}

                {% if app.user.NombreImgPerfil is null %}
                    <a class="btn btn-sm btn-light m-1" href="{{ path('session_cuenta') }}">Cuenta</a>
                {% else %}
                    <a class="btn btn-sm btn-light m-1" href="{{ path('session_cuenta') }}">
                        <img style="width:50px;height:50px;" src="{{ asset('uploads/perfil/' ~ app.user.NombreImgPerfil) }}">
                    </a>
                {% endif %}
                <a class="btn btn-sm btn-light m-1" href="{{ path('app_logout') }}">Logout</a>
            {%  else  %}
                <a class="btn btn-sm btn-light m-1" href="{{ path('login') }}">Login</a>
            {% endif %}
        </div>
    </div>
</nav>